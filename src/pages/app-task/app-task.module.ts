import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppTaskPage } from './app-task';

@NgModule({
  declarations: [
    // AppTaskPage,
  ],
  imports: [
    // IonicPageModule.forChild(AppTaskPage),
  ]
})
export class AppTaskPageModule {}
