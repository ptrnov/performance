import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppformAuthenticationPage } from './appform-authentication';

@NgModule({
  declarations: [
    // AppformAuthenticationPage,
  ],
  imports: [
    // IonicPageModule.forChild(AppformAuthenticationPage),
  ],
})
export class AppformAuthenticationPageModule {}
